{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./build",
    "baseUrl": "."
  },
  "include": [ "**/*.ts", "**/*.json" ],
  "exclude": [
    "build"
  ]
}
